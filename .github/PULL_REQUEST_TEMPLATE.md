## Summary

What does this change and why?

## Checklist

- [ ] `pytest` passes (deterministic suite)
- [ ] `ruff check .` and `ruff format --check .` pass
- [ ] `mypy prompt_enhancer` passes
- [ ] Added/updated tests for the change
- [ ] Updated `CHANGELOG.md` under `[Unreleased]`
- [ ] Preserves fail-open behavior and never writes prompt contents to disk/logs by default
